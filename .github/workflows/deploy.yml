# This workflow is deprecated. See autonomous_agent.yml for the 24/7 agent.
# Keeping this file to prevent errors from pushes.
name: "Deploy OpenCLAW Literary Agent"
on:
  push:
    branches: [main]
jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - run: echo "✅ Code pushed. Autonomous agent runs via cron every 3 hours."
